# tests/parity

Spec 012 — structural/token parity assertions per area (states present, expected tokens/icons/structure). Parity match itself is review-signed-off via `specs/012-design-parity-audit/inventory.md` (no pixel-diff).
