// Cognitive MCQ — single-select option cards. Stores the chosen option key (US2 / FR-USR-008).
import { OptionGroup, type RendererProps } from './shared';

export function McqRenderer(props: RendererProps) {
  return <OptionGroup {...props} />;
}
