// Situational Judgement Test — scenario text + single-select option cards. Stores the option key (US2).
import { OptionGroup, type RendererProps } from './shared';

export function SjtRenderer(props: RendererProps) {
  return <OptionGroup {...props} />;
}
